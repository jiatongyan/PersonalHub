<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.dylan.personalhub.mapper.EducationMapper">


    <select id="findAll"
            resultType="com.dylan.personalhub.entity.Education">


        SELECT

            id,
            school,
            major,
            degree,
            start_time,
            end_time,
            description

        FROM education

        ORDER BY start_time DESC


    </select>

    <select id="findById"
            resultType="com.dylan.personalhub.entity.Education">

        SELECT

            id,
            school,
            major,
            degree,
            start_time,
            end_time,
            description

        FROM education

        WHERE id = #{id}

    </select>


    <insert id="insert">
        INSERT INTO education
        (
            school,
            major,
            degree,
            start_time,
            end_time,
            description
        )
        VALUES
            (
                #{school},
                #{major},
                #{degree},
                #{startTime},
                #{endTime},
                #{description}
            )
    </insert>


    <update id="update">
        UPDATE education
        SET
            school      = #{school},
            major       = #{major},
            degree      = #{degree},
            start_time  = #{startTime},
            end_time    = #{endTime},
            description = #{description}
        WHERE id = #{id}
    </update>


    <delete id="deleteById">
        DELETE FROM education
        WHERE id = #{id}
    </delete>



</mapper>